# Overview

This repository exposes a set of utilities to launch and manage Zcash processes. In addition, it provides a binary
that launches a `zebrad` and a `zainod` process in regtest.

These should be present in the global PATH env for this program to pick them up.
